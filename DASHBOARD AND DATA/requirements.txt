# AeroTwin-4 dependency manifest.
#
# scikit-learn is pinned, not floated: IsolationForest's tree construction changed
# between 1.6 and 1.9, which moves the reported ablation F1 by a wide margin. Pin it
# or the documented numbers cannot be reproduced.

numpy>=1.26,<3
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
pyarrow>=14.0            # parquet telemetry export (silently degrades to CSV without it)

scikit-learn==1.6.1      # PINNED - see note above
joblib>=1.3
torch>=2.2               # CPU build is sufficient: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Real-time API layer
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0

pytest>=7.0
httpx>=0.27              # FastAPI TestClient transport
