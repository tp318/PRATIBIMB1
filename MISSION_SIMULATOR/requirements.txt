# Mission Simulator — Python dependencies
# Install with: pip install -r requirements.txt
#
# Core scientific stack
numpy>=1.26,<3.0
pandas>=2.0

# Web framework + async server
fastapi>=0.110
uvicorn[standard]>=0.29

# WebSocket support (used by the websockets library internally)
websockets>=12.0

# HTTP request body parsing for FastAPI POST endpoints
python-multipart>=0.0.9
