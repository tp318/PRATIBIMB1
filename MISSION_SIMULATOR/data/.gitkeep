# This file keeps the data/ directory tracked by git.
# Mission CSV logs (mission_YYYYMMDD_HHMMSS.csv) are written here at runtime
# and are intentionally excluded from version control via .gitignore.
