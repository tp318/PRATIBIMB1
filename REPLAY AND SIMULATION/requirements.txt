# Requirements for REPLAY AND SIMULATION standalone package
numpy>=1.20.0
pandas>=1.3.0
# PostgreSQL support (optional; package automatically falls back to built-in SQLite if absent)
psycopg2-binary>=2.9.0
pytest>=7.0.0
